#include <stdio.h>
int main(void)
{
        char st[10];
        printf("entrer le nom de la personne\n");
        scanf("%10s",st); 
        printf("le nom de la personne est %s\n",st);
	return 1;
}
